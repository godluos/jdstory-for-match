<nav class="navbar navbar-default navbar-fixed white no-background bootsnav navbar-mobile">
  <div class="call-us-area bg-info text-white">
    <div class="container">
      <div class="row">
        <div class="col-sm-6 col-xs-6">
          <div class="call-us-number">
            <ul class="list-inline">
              <li><span class="icon_mail_alt"></span>艺汇—装饰的品味</li>
            </ul>
          </div>
        </div>
        <div class="col-sm-6 col-xs-6">
          <div class="call-us-text text-right">
            <% if !current_user %>
              <a><%= link_to("注册", new_user_registration_path, class: 'hello') %> </a>
              <a><%= link_to(content_tag(:i, '登入', class: 'fa fa-sign-in'),
                 new_user_session_path, class: 'hello') %></a>
            <% else %>
              <%= link_to("个人列表", account_users_path, class:"hello" ) %>
              <a class="hello">欢迎，<%= current_user.username %></a>
              <% if current_user.admin? %>
                  <%= link_to("Admin 选单", admin_products_path,class:"hello text-right" ) %>
              <% end %>
            <% end%>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="hv2-main-menu-bg">
    <div class="container">
      <div class="row">
        <div class="container">
          <div class="row">
            <!-- Start Header Navigation -->
            <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-menu">
                <i class="fa fa-bars"></i>
              </button>
              <a class="navbar-brand" href="#">
                <!-- <img src="assets/logov1.png" class="logo logo-display" alt="">
                <img src="assets/logoscroll.png" class="logo logo-scrolled" alt=""> -->
              </a>
            </div>
            <!-- End Header Navigation -->
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="navbar-menu">
              <ul class="nav navbar-nav navbar-right" data-in="fadeIn" data-out="fadeOut">
                <li>
                  <%= link_to("返回首页",root_path)%>
                </li>
                <li>
                  <%= link_to("艺术版画",products_path(category:"艺术版画"))%>
                </li>
                <li>
                  <%= link_to("工艺墙饰",products_path(category:"工艺墙饰"))%>
                </li>
                <li>
                  <%= link_to("创意摆件",products_path(category:"创意摆件"))%>
                </li>
                <li>
                  <%= link_to carts_path do  %>
                    购物车 <i class="fa fa-shopping-cart"> </i>
                    ( <%= current_cart.products.count %> )
                  <% end %>
                </li>
              </ul>
            </div>
            <!-- /.navbar-collapse -->
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
